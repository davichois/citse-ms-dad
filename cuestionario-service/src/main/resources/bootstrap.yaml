spring:
  application:
    name: cuestionario-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  jpa:
    hibernate:
      ddl-auto: none

  datasource:
    url: jdbc:mysql://localhost:3306/bdo_cuestionario
    username: root
    password: davidprada27

